<UserControl x:Class="RestauranteGestion.Views.VentaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="730.699" d:DesignWidth="1000">

    <Grid Background="#FFF5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Encabezado -->
        <Border Grid.Row="0" Background="#FF3F51B5" Padding="20">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon Kind="ChartBoxOutline" Foreground="White" Width="32" Height="32" Margin="0,0,10,0"/>
                <TextBlock Text="GESTIÓN DE VENTAS" 
                           FontSize="22" 
                           FontWeight="SemiBold" 
                           Foreground="White"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Contenido Principal -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <!-- Filtros -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
                <DatePicker x:Name="FilterDate"
                            Width="200"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            materialDesign:HintAssist.Hint="Filtrar por fecha" />

                <Button Content="Filtrar"
                        Command="{Binding FilterCommand}"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="16,0,0,0"
                        Background="#FF3F51B5"
                        Foreground="White" />
            </StackPanel>

            <!-- Lista de Ventas -->
            <Border Grid.Row="1" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4">
                <DataGrid ItemsSource="{Binding VentasPaginadas}"
                          SelectedItem="{Binding SelectedVenta, Mode=TwoWay}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          GridLinesVisibility="None"
                          RowBackground="White"
                          AlternatingRowBackground="#FFF5F5F5"
                          BorderThickness="0"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding IdVenta}" Width="80"/>
                        <DataGridTextColumn Header="FECHA" Binding="{Binding FechaHora, StringFormat='{}{0:dd-MM-yyyy HH:mm}'}" Width="*"/>
                        <DataGridTextColumn Header="EMPLEADO" Binding="{Binding Empleado.NombreCompleto}" Width="200"/>
                        <DataGridTextColumn Header="TOTAL" Binding="{Binding Total, StringFormat=C}" Width="100"/>
                        <DataGridTextColumn Header="MÉTODO DE PAGO" Binding="{Binding MetodoPago}" Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- Detalles de Venta -->
            <Border Grid.Row="2" BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="4" Margin="0,10,0,0"
                    Background="{DynamicResource MaterialDesignPaper}"
                    Visibility="{Binding SelectedVenta}">
                <DataGrid ItemsSource="{Binding SelectedVenta.Detalles}"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          MaxHeight="250"
                          Style="{StaticResource MaterialDesignDataGrid}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*"/>
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="100"/>
                        <DataGridTextColumn Header="Precio Unitario" Binding="{Binding PrecioVenta, StringFormat=C}" Width="130"/>
                        <DataGridTextColumn Header="Subtotal" Binding="{Binding SubTotal, StringFormat=C}" Width="130"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

       

        <!-- Botones de acción -->
        <Border Grid.Row="3" Background="White" Padding="10" BorderThickness="0,1,0,0" BorderBrush="#FFE0E0E0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="NUEVA VENTA" Command="{Binding NuevaVentaCommand}" Margin="5,0"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Background="#FF3F51B5" Foreground="White" Width="140" Height="36"/>

                <Button Content="EDITAR" Command="{Binding EditarVentaCommand}" Margin="5,0"
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        BorderBrush="#FF3F51B5" Foreground="#FF3F51B5" Width="120" Height="36"/>
                <Button Content="ELIMINAR" 
                        Command="{Binding DeleteCommand}" 
                        Margin="5,0" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        BorderBrush="#FFE53935"
                        Foreground="#FFE53935"
                        Width="120"
                        Height="36"/>
            </StackPanel>
        </Border>
        <!-- Controles de Paginación -->
        <Border Grid.Row="2" Background="White" Padding="10" BorderThickness="0,1,0,0" BorderBrush="#FFE0E0E0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                <!-- Botón de Anterior -->
                <Button Content="Anterior" Command="{Binding PreviousPageCommand}" />

                <!-- Indicador de Página Actual -->
                <TextBlock Text="{Binding PaginationInfo}" VerticalAlignment="Center" FontWeight="SemiBold" Margin="10,0" />


                <!-- Botón de Siguiente -->
                <Button Content="Siguiente" Command="{Binding NextPageCommand}" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
