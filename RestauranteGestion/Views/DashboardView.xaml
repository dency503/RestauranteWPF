<UserControl x:Class="RestauranteGestion.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:local="clr-namespace:RestauranteGestion.Converters"

             mc:Ignorable="d" 
             d:DesignHeight="768" d:DesignWidth="1366" Background="White"
             Loaded="UserControl_Loaded">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Sección de Métricas Principales -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <materialDesign:Card Margin="5" Width="250" Padding="15">
                    <StackPanel Orientation="Horizontal" >
                        <materialDesign:PackIcon Kind="Cash" Width="40" Height="40" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="Ventas Hoy" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
                            <TextBlock Text="{Binding VentasHoy}" FontSize="22" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Margin="5" Width="250" Padding="15">
                    <StackPanel Orientation="Horizontal" >
                        <materialDesign:PackIcon Kind="Table" Width="40" Height="40" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="Mesas Ocupadas" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
                            <TextBlock Text="{Binding MesasOcupadas}" FontSize="22" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Margin="5" Width="250" Padding="15">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Alert" Width="40" Height="40" Margin="0,0,10,0"/>
                        <StackPanel>
                            <TextBlock Text="Alertas Inventario" Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
                            <TextBlock Text="{Binding AlertasInventario}" FontSize="22" FontWeight="Bold"/>
                        </StackPanel>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>

            <!-- Gráficos Principales -->
            <Grid Grid.Row="1" Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:Card Grid.Column="0" Margin="5">
                    <lvc:CartesianChart Series="{Binding VentasSeries}" LegendLocation="Bottom" Height="300">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Title="Fecha" Labels="{Binding Fechas}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis Title="Ventas ($)"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1" Margin="5">
                    <lvc:PieChart Series="{Binding TopProductosSeries}" LegendLocation="Bottom" Height="300"/>
                </materialDesign:Card>
            </Grid>

            <!-- Estado de Mesas -->
            <materialDesign:Card Grid.Row="2" Margin="5" Padding="10">
                <StackPanel>
                    <TextBlock Text="Estado de Mesas" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <ItemsControl ItemsSource="{Binding Mesas}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="80" Height="80" Margin="5" 
                            CornerRadius="5" >
                                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="{Binding NumeroMesa}" HorizontalAlignment="Center" 
                                       FontSize="18" FontWeight="Bold" />
                                        <TextBlock Text="{Binding Estado}" HorizontalAlignment="Center" 
                                       FontSize="12"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </materialDesign:Card>


            <!-- Últimos Pedidos y Alertas -->
            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <materialDesign:Card Grid.Column="0" Margin="5" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Últimos Pedidos" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding UltimosPedidos}" AutoGenerateColumns="False"
                                 HeadersVisibility="Column" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Mesa" Binding="{Binding Mesa.NumeroMesa}"/>
                                <DataGridTextColumn Header="Cliente" Binding="{Binding NombreCliente}"/>
                                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat=C}"/>
                                <DataGridTextColumn Header="Estado" Binding="{Binding Estado}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>

                <materialDesign:Card Grid.Column="1" Margin="5" Padding="10">
                    <StackPanel>
                        <TextBlock Text="Inventario Crítico" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <DataGrid ItemsSource="{Binding InventarioCritico}" AutoGenerateColumns="False"
                                 HeadersVisibility="Column" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Ingrediente" Binding="{Binding NombreIngrediente}"/>
                                <DataGridTextColumn Header="Stock Actual" Binding="{Binding Stock}"/>
                                <DataGridTextColumn Header="Mínimo Requerido" Binding="{Binding StockMinimo}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </materialDesign:Card>
            </Grid>
        </Grid>
    </ScrollViewer>
    <UserControl.Resources>
        <local:EstadoMesaToBrushConverter x:Key="EstadoMesaToBrushConverter"/>
    </UserControl.Resources>

</UserControl>